@model ProductEditVM

@{
    ViewBag.Title = "Product";
}

<h1>Edit</h1>

<h4>Product</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit" method="post" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
          
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Price" class="control-label"></label>
                <input asp-for="Price" class="form-control" />
                <span asp-validation-for="Price" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Desc" class="control-label"></label>
                <input asp-for="Desc" class="form-control" />
                <span asp-validation-for="Desc" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label for="3">Category</label>
                <select asp-for="CategoryId" asp-items="@new SelectList(Model.Categories, nameof(Category.Id), nameof(Category.Name))" id="3" class="form-control">
                    <option disabled selected>Choose category</option>
                </select>
                <span asp-validation-for="CategoryId" class="text-danger"></span>
            </div>
            <br />
            <div class="form-group">
                <label for="4">Brand</label>
                <select asp-for="BrandId" asp-items="@new SelectList(Model.Brands, nameof(Brand.Id), nameof(Brand.Name))" id="4" class="form-control">
                    <option disabled selected>Choose Brand</option>
                    <span asp-validation-for="BrandId" class="text-danger"></span>
                </select>
            </div>
            <br />
            <div class="form-group">
                <label for="6">Color</label>
                <select asp-for="ColorId" asp-items="@new SelectList(Model.Colors, nameof(Color.Id), nameof(Color.Name))" id="6" class="form-control">
                    <option disabled selected>Choose Color</option>
                </select>
                <span asp-validation-for="ColorId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label for="5">Type</label>
                <input multiple asp-for="Images" type="file" class="form-control img-input" id="5">
                <span asp-validation-for="Images" class="text-danger"></span>
               
                <div class="img-preview-container">
                    @for (int i = 0; i < Model.AllImages.Count; i++)
                    {
                        var imagePath = Model.AllImages[i];
                        <div class="img-preview">
                            <img src="~/img/product-img/@imagePath.Url" style="width:200px" />
                            @if (imagePath != null)
                            {
                                <a href="#" class="delete-btn btn btn-danger" data-imageid="@imagePath.Id">Delete Image</a>
                            }
                        </div>
                        <input type="hidden" name="ImagesId" value="@imagePath.Id" />
                    }
                </div>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="InStock" /> @Html.DisplayNameFor(model => model.InStock)
                </label>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        $(document).ready(function () {
            $('.delete-btn').on('click', function () {
                var imageId = $(this).data('imageid');
                $('<input>').attr({
                    type: 'hidden',
                    name: 'DeletedImageIds',
                    value: imageId
                }).appendTo('form');
                $(this).closest('.img-preview').remove();
            });
        });
    </script>
    <script src="~/admin/assets/js/ImageDelete.js"></script>
}
